# StreamDirector — English (source locale).
# Format OBS : Key="Value". obs_module_text("Key") renvoie la valeur de la locale
# active ; cette locale en-US sert de fallback si une cle manque ailleurs.
# IMPORTANT : garder EXACTEMENT le meme jeu de cles que les autres locales (fr-FR).

# --- Dock : en-tete ---
Dock.Title="StreamDirector"
Dock.Status.Active="Active"
Dock.Status.Paused="Paused"
Dock.Status.ReadOnly="Read-only"

# --- Sections ---
Section.Speakers="SPEAKERS"
Section.Directing="DIRECTING"

# --- Intervenants ---
Speaker.State.Speaking="SPEAKING"
Speaker.State.Silent="silent"
Speaker.Threshold="Threshold"

# --- A l'antenne ---
OnAir.Label="On air"
OnAir.Unavailable="(unavailable)"

# --- Pilotage ---
Control.WideShot="Wide shot"
Control.AutoOn="Auto ON"
Control.AutoOff="Auto OFF"
Control.ToggleAuto="Enable / disable automatic directing"

# --- Bandeau Stream Deck ---
StreamDeck.Hint="Stream Deck: map these shortcuts in Settings > Hotkeys."

# --- Footer ---
Footer.AdvancedSettings="Advanced settings"
Footer.Assistant="Assistant"
Footer.ComingSoon="Coming soon"

# --- Mode / config ---
Mode.Auto.On="AUTOMATIC DIRECTING active — StreamDirector switches your scenes by who speaks."
Mode.Auto.Off="Directing paused — enable automatic directing to start."
Mode.ReadOnly="Read-only: no config loaded. Add a config.json to drive scenes."
Config.Loaded="Config loaded — %1 speaker(s)"
Config.Invalid="Config INVALID — %1"
Config.Missing="No config (read-only). Place config.json here:"
Config.Refresh="Refresh (sources + config)"

# --- Divers ---
Empty.NoSources="No audio source detected. Add sources in OBS then Refresh."
